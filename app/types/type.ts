export type menuItemType = {
  label: string;
  id: string;
};

export type ReformerBenefitsType = {
  title: string;
  subTitle: string;
  text: string;
  image: {
    src: string;
    alt: string;
  };
};
